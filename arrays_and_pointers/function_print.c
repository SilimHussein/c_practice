#include <stdio.h>
#include "main.h"
/**
 * f - prints the value of a pointer of type int
 * @a: address of an integer we need to print
 *
 * Return: void
 */
void f(int *a)
{
	printf("a: %p\n", a);
}
