#include <stdio.h>
/**
 * main- has do while loop, executes the program atleast once
 *
 * Return: Always 0
 */
int main(void)
{
	int i = 0;

	do {
		printf("Silim \n");
	} while (i > 0);
	printf("out of loop \n");
	return (0);
}
